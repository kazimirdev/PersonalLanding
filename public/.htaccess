Options -MultiViews
RewriteEngine On

# If file or dir exists - keep it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send all app routes to front controller
RewriteRule ^ index.php [QSA,L]
